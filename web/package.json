{
  "name": "multimodal-visual-search-web",
  "private": true,
  "description": "Static CLIP retrieval demo deployed to Cloudflare",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "models:pull": "node scripts/models.mjs pull",
    "models:push": "node scripts/models.mjs push",
    "models:push:local": "node scripts/models.mjs push --local"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
